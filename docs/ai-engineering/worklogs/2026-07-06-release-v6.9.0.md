# 2026-07-06: Release v6.9.0 (estreia do RELEASE-CHECKLIST)

**Executor**: Claude Fable 5 via Claude Code
**Objetivo**: publicar a v6.9.0 cobrindo 25 commits acumulados desde a v6.8.0 (camada ai-engineering, distribuição Codex, /otimizar-copy, /narrar-roteiro, /datas-sazonais, memory no analytics, gates de antítese, P0 da auditoria).
**Fora de escopo declarado**: onda P1 de paridade; qualquer feature nova.

## Evidência por fase do checklist

| Fase | Item | Evidência |
|---|---|---|
| 0 | Tree limpo, main | `git status` vazio, branch main |
| 0 | Suite | 1977 passed, 2 skipped (pré e pós-bump) |
| 0 | validate_agents --strict | 18/18 clean |
| 0 | Drift scan | 38 commands, 18 agents, 34 clones conferidos contra filesystem |
| 1 | Range revisado | `git log v6.8.0..HEAD`: 23 commits + 2 da sessão |
| 1 | CHANGELOG | Seção `[Unreleased]` (Codex) fundida na v6.9.0; Added/Changed/Fixed cobrem o range inteiro |
| 1 | Semver | Minor (features novas, sem breaking): 6.9.0 |
| 2 | Bump 4 pontos + badge | plugin.json, marketplace.json (2), codex `6.9.0+codex.20260706`, badge README |
| 2 | `claude plugin validate .` | Passed com 1 warning novo: `category` em plugin.json "belongs in marketplace entry, harmless/unused". Mantido como está (remover contradiz o gotcha histórico do Desktop); reavaliar no próximo ciclo |
| 3 | Build + validate Codex | `build_codex_plugin.py` ok; `validate_codex_plugin.py plugins/marketing-os` passed (o script exige o path como argumento; checklist anotado mentalmente, corrigir exemplo na próxima edição) |
| 3 | Anti-vazamento | find por workspace/memory/narracao no pacote: 0 ocorrências |
| 4 | Commit release | `f7bbc21 release: v6.9.0` |
| 4 | Ancestralidade | `git merge-base --is-ancestor v6.9.0 main` → OK (guard F-REL-01) |
| 4 | Push | main `4c2c09b..f7bbc21` + tag v6.9.0 confirmada no remoto via `ls-remote` |
| 5 | Install real Claude | **PENDENTE (humano)**: `/plugin install marketing-os@mos-marketplace` em projeto limpo + rodar 1 command e 1 caso da camada viva do golden set |
| 5 | Install real Codex | **PENDENTE (humano)**: `codex plugin marketplace add rilnermucio/esp-marketing-os --ref v6.9.0` + install |
| 6 | Worklog + rubrica | Este arquivo |

## Decisões
- Skip gracioso no `test_routing_evals.py` quando `docs/ai-engineering/` não existe: o pacote Codex copia `scripts/` mas não `docs/`, e o load module-level quebraria a coleta da suite inteira dentro do pacote. No repo o guard segue estrito (JSON ausente com o diretório presente ainda quebra alto).
- Warning novo do validator sobre `category`: não remover nesta release. O gotcha histórico (Desktop) pedia o campo; o validator atual diz que é ignorado. Mudar campo de manifest sem teste real de install é exatamente o erro da era v6.1.x.

## Rollback (anotado antes do push, executável se necessário)
```bash
git tag -d v6.9.0 && git push origin :refs/tags/v6.9.0   # despublica a tag
git revert f7bbc21                                        # reverte o release commit
```
Marketplace Claude pega o estado do repo no próximo sync; Codex por `--ref` fixa não é afetado retroativamente.

## Falhas da taxonomia tocadas
- Prevenidas nesta execução: F-REL-01 (ancestralidade verificada), F-REL-02 (CHANGELOG cobre o range), F-MAN-02 (4 pontos de versão no mesmo commit), F-CODEX-01/02/03 (validate + sufixo + anti-vazamento)
- Pendência consciente: F-REL-03 fica aberta até os installs reais da Fase 5 (humano)

## Rubrica aplicada (R6 Release)
- Checklist: 4 | Tag: 4 | CHANGELOG: 4 | Install real: 2 (metade pendente, registrada) | Rollback: 4
- Nota: R6 exige tudo ≥3; o item install está em 2 COM registro explícito e dono (usuário). Release publicada com essa pendência declarada por decisão do mantenedor humano ("Pode fazer a release").

## Custo aproximado
- Fração de sessão de fronteira; 15 arquivos no commit de release; zero re-leituras de KB. Não medido em tokens.

## Próximos passos
1. **Fase 5 (humano)**: os 2 installs reais acima; reportar resultado pra fechar F-REL-03
2. Corrigir exemplo do validate_codex no RELEASE-CHECKLIST (falta o argumento `plugins/marketing-os`)
3. Onda P1 de paridade (mos-email, mos-seo) medida pela matriz + rubricas
4. Primeira execução da camada viva dos routing evals
5. mos-offer (ROADMAP Fase 3)
