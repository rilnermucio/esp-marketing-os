# 2026-07-06: Execução do P0 da auditoria

**Executor**: Claude Fable 5 via Claude Code
**Objetivo**: fechar o backlog P0 da auditoria de jun/2026 (drift de contagens, referência stale, dependência não declarada, higiene de repo).
**Fora de escopo declarado**: release v6.9.0 (próxima rodada); onda P1 de paridade; qualquer refactor.

## Arquivos lidos (relevantes pra decisão)
- Scout de re-verificação: greps de contagens em README/AGENTS/SKILL, conteúdo de `narracao.aiff.txt` (13 bytes: "teste de rota"), `docs/CONNECTORS.md` completo, seção de fallback do `commands/criar-post.md`
- Motivo do scout: README tinha sido editado externamente (Codex install); parte do P0 podia já estar resolvida. Estava: linha de scripts do README já sem número; "36 perfis" já normalizado (mas para 34, valor errado).

## Correção durante a rodada (o guard pegou o mantenedor)

A auditoria de jun/2026 afirmava 35 voice clones ("36 diretórios menos design"). O guard `test_repo_consistency::test_readme_counts_match_filesystem` reprovou a mudança 34→35 e a re-verificação mostrou o off-by-one: o `ls` de junho contava 36 ENTRADAS, uma delas o arquivo `clone-manifest.yaml`. Diretórios reais: 35, menos `design/` = **34**. O README estava certo; a auditoria estava errada; o mesmo off-by-one existia desde antes em `agents/mos-copy.md` ("35 clones", "Mais 25 modernos") e na PARTE XV-B do `copy-agent.md` (subtotal "Business Strategists (2)" com 1 nome; "35 × 4 = 140"). Tudo corrigido pra 34 nesta rodada. Lição registrada: contagem de evidência usa `ls -d */` (diretórios), não `ls | wc -l`; e claim de auditoria também é claim (princípio 4 vale pra dentro).

## Arquivos alterados/criados
- `README.md`: clones corrigidos pra 34 (tagline, árvore de estrutura, header da seção; o valor 34 é o validado pelo guard)
- `AGENTS.md`: clones 34 mantido/corrigido; contagem de scripts REMOVIDA em 2 pontos (frase sem número, H8.5)
- `agents/mos-copy.md`: "35 clones"→34; "Mais 25 clones modernos"→24
- `subagents/copy-agent.md`: PARTE XV-B com subtotal Business Strategists (1), aviso inline e total 34×4=136
- `skills/marketing-os/SKILL.md`: memory 9→10; `mos-analytics` movido pra lista com memory; "outros 8" corrigido
- `commands/criar-post.md`: bloco de fallback de migração removido (mos-social existe desde v6.x)
- `requirements.txt`: numpy declarado (import direto em audit_radar_chart.py)
- `docs/CONNECTORS.md`: nome pessoal "(Especializei)" removido do connector Meta Ads; nota esclarecendo que connectors são MCPs instalados pelo usuário
- `narracao.aiff.txt`: removido do repo via `git rm` (13 bytes de teste, sem valor)

## Decisões (e alternativas rejeitadas)
- **Contagem de scripts: remover o número em vez de atualizar.** O valor mudou 3x em 30 dias (48→50→52). Atualizar seria re-agendar o mesmo bug (H8.5). Alternativa "guard test de contagem" rejeitada: o número não carrega informação que justifique manutenção.
- **CONNECTORS.md: corrigir, não deletar.** A auditoria sugeria que o connector Meta "não existe"; na verdade todos os connectors listados são MCPs do ambiente do usuário. O problema real era o nome pessoal num doc distribuído. Corrigido com nota de contexto.
- **narracao.aiff.txt: delete direto (sem mover pra workspace/).** Conteúdo inspecionado antes: 13 bytes de texto de teste. Autorização coberta pelo "pode executar" do P0, que listava o item nominalmente.

## Evidências
- Conteúdo do arquivo removido verificado antes do rm: `wc -c` = 13; conteúdo "teste de rota"
- Clones: `ls -d assets/clones/*/ | grep -v design | wc -l` = 34; guard `test_repo_consistency` de acordo; inventário da PARTE XV-B soma 10+12+8+3+1 = 34
- `grep "memory: project" agents/*.md` na auditoria: 10 agents, incluindo mos-analytics (commit 16e79ae)

## Testes
- Rodados: suite completa `-m "not smoke"` + `validate_agents.py --strict` (resultado no fechamento da rodada; criar-post continua passando os contratos de dispatch porque o bloco removido era redundante)
- Não rodados e motivo: smoke (tokens); install (sem release nesta rodada)

## Falhas da taxonomia tocadas
- Corrigidas: F-DOC-01 (contagens de clones/scripts/memory), F-CMD-02 (fallback stale), F-CODEX-03 (arquivo pessoal na raiz, que era candidato a vazar em empacotamentos futuros)
- Prevenção estrutural: 2 contagens convertidas em frase sem número

## Rubrica aplicada
- R1 Implementação: 3 (fixes verificados por scout prévio e suite; sem guard novo porque os itens ou viraram frase sem número ou já têm detecção por auditoria)
- R2 Documentação: 4 (todos os pontos de sincronia da tabela do handbook cobertos no mesmo diff; worklog no fechamento)
- R3/R4/R5/R6: N/A (sem mudança de roteamento, output, manifests ou release)
- Veredito de merge: aprovado

## Custo aproximado
- Fração de sessão de fronteira; 7 arquivos alterados + 1 removido; scout de 2 comandos + 4 leituras dirigidas. Tokens não expostos: "não medido".

## Riscos e follow-ups
- A contagem "35 clones" volta a driftar quando entrar clone novo: mitigado pela tabela de sincronia do MAINTAINER-HANDBOOK (clone novo → manifest + PARTE XV-B + contagem)
- `.agents/plugins/` e `plugins/marketing-os/` (pacote Codex commitado) aumentam a superfície de sincronia: o pacote precisa ser regenerado a cada release (já coberto pela Fase 3 do RELEASE-CHECKLIST)

## Próximos passos
1. **Release v6.9.0** pelo RELEASE-CHECKLIST (estreia; CHANGELOG cobre v6.8.0..HEAD, que agora inclui camada ai-engineering + este P0)
2. **Onda P1**: paridade mos-email e mos-seo primeiro (memory + Bash + auto-iteração + pre-flight), medida pela matriz da auditoria + rubricas
3. **Camada viva dos routing evals**: primeira execução com 5+ casos
4. **mos-offer** (ROADMAP Fase 3)
