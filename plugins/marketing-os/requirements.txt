# ============================================================
# Marketing OS - Dependências Python
# ============================================================
#
# A maioria dos scripts utiliza apenas módulos da biblioteca padrão
# do Python 3.8+. Algumas funcionalidades específicas requerem
# dependências opcionais listadas abaixo.
#
# Módulos padrão utilizados pelos scripts:
#   - json          (seo_analyzer, hashtag_generator, ab_generator,
#                    content_calendar, hook_generator, content_idea_generator)
#   - sys           (todos os scripts)
#   - re            (seo_analyzer, ab_generator, headline_scorer,
#                    readability_checker, content_repurposer)
#   - os            (uso geral)
#   - datetime      (content_calendar, content_repurposer,
#                    content_idea_generator)
#   - argparse      (headline_scorer, readability_checker,
#                    content_repurposer)
#   - collections   (seo_analyzer, readability_checker - Counter)
#   - random        (hook_generator, content_idea_generator)
#   - statistics    (readability_checker)
#   - typing        (headline_scorer, readability_checker,
#                    content_repurposer, hook_generator,
#                    content_calendar, content_idea_generator)
#
# ============================================================
# Dependências requeridas por scripts específicos
# ============================================================
# tiktok_trends_scraper.py:
# TikTok-Api>=7.0.0           # Scraping de tendências do TikTok
# playwright>=1.40.0           # Automação de navegador para TikTok-Api
#
# ============================================================
# Dependências opcionais para funcionalidades futuras
# ============================================================
# Descomente as linhas abaixo conforme necessário:
#
# requests>=2.28.0          # Para integração com APIs externas
# beautifulsoup4>=4.12.0    # Para web scraping de conteúdo
# pandas>=2.0.0             # Para análise de dados e métricas
# matplotlib>=3.7.0         # Para geração de gráficos e relatórios
# openai>=1.0.0             # Para integração com API da OpenAI
# anthropic>=0.18.0         # Para integração com API da Anthropic
# tweepy>=4.14.0            # Para integração com Twitter/X API
# python-dotenv>=1.0.0      # Para gerenciamento de variáveis de ambiente
# schedule>=1.2.0           # Para agendamento de publicações
# Pillow>=10.0.0            # Para manipulação de imagens

# ============================================================
# Dependências para /auditoria (multi-modal PDF report)
# ============================================================
# Teto no próximo major pra evitar breakage silencioso em upgrade (reproducibilidade).
weasyprint>=60.0,<73
markdown-it-py>=3.0,<5
jsonschema>=4.0,<5

# Dependencias para /auditoria-pro v6.8.0
matplotlib>=3.7,<4
playwright>=1.40,<2
